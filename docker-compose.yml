services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sudyod_mortor_fe
    ports:
      - "8099:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_BASE_URL=/api/v1
      - API_TARGET=http://sudyod_mortor_v2-app-1:8009
    networks:
      - sudyod_mortor_v2_default

networks:
  sudyod_mortor_v2_default:
    external: true
